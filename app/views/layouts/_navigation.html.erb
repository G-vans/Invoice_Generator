<nav class="bg-white shadow-sm border-b border-gray-200">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex">
        <div class="flex-shrink-0 flex items-center">
          <%= link_to root_path, class: "text-xl font-bold text-blue-600" do %>
            Invoice Generator
          <% end %>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <%= link_to "Invoices", root_path, class: "inline-flex items-center px-1 pt-1 border-b-2 border-transparent text-sm font-medium text-gray-500 hover:text-gray-700 hover:border-gray-300 #{'border-blue-500 text-gray-900' if controller_name == 'invoices'}" %>
          <%= link_to "Products", products_path, class: "inline-flex items-center px-1 pt-1 border-b-2 border-transparent text-sm font-medium text-gray-500 hover:text-gray-700 hover:border-gray-300 #{'border-blue-500 text-gray-900' if controller_name == 'products'}" %>
          <%= link_to "Settings", edit_settings_path, class: "inline-flex items-center px-1 pt-1 border-b-2 border-transparent text-sm font-medium text-gray-500 hover:text-gray-700 hover:border-gray-300 #{'border-blue-500 text-gray-900' if controller_name == 'settings'}" %>
        </div>
      </div>
    </div>
  </div>
</nav>

